apiVersion: v1
kind: Ingress
metadata:
  labels:
    io.service: enqualify-ui
  name: enqualify-ui
  namespace: {{ .Values.namespace }}
spec:
  selector:
    io.service: enqualify-ui
  ingressClassName: {{ .Values.ingressClassName }}
  rules:
  - host: 
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: driver
            port:
              number: 80